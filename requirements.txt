# Hailo Whisper Transcription - Official Dependencies
# Python 3.11 required for Hailo compatibility
#
# CRITICAL: NumPy 1.24.2 required for Hailo C++ bindings compatibility!
# NumPy 2.x has breaking changes to C API that cause "buffer size 0" errors
# Use venv with --system-site-packages to access hailo_platform bindings

# PyTorch (required by common.audio_utils from Hailo examples)
torch==2.6.0

# Core numerical computing - MUST be 1.24.2 for Hailo compatibility!
numpy==1.24.2

# Audio processing
soundfile>=0.12.0
scipy==1.9.3

# Terminal UI
simple-term-menu>=1.6.0

# Transformers (for Whisper tokenization)
transformers==4.50.1

# Transformers dependencies (minimal set)
packaging>=20.0
pyyaml>=5.1
regex>=2019.12.18
tokenizers>=0.21.0,<0.22.0
safetensors>=0.4.0
tqdm>=4.27
huggingface-hub>=0.34.0,<1.0.0
filelock>=3.0.0
requests>=2.28.0
fsspec>=2023.5.0
hf-xet>=1.1.0
typing-extensions>=4.5.0
