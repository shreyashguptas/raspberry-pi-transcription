# Voice Transcription Requirements
# Python 3.13+

# Audio processing
soundfile>=0.12.1
scipy>=1.14.0

# Whisper model for transcription
faster-whisper>=1.0.3

# NumPy (required by scipy)
numpy>=1.26.0

# Interactive CLI menu system
simple-term-menu>=1.6.1

# System dependencies (install via apt):
# - libasound2-dev (ALSA sound library)
# - libportaudio2 (PortAudio)
# - libsndfile1 (libsndfile for soundfile)
# - python3-pyaudio (PyAudio for ALSA)
#
# For building from source (if pre-built wheels not available):
# - gfortran (Fortran compiler for scipy)
# - libopenblas-dev (Optimized BLAS library)
# - liblapack-dev (Linear algebra package)

# ============================================================================
# HAILO AI HAT DEPENDENCIES (for transcribe-halo.py)
# ============================================================================
#
# To use hardware-accelerated transcription with the Hailo AI HAT:
#
# 1. Install hailo-all package (includes HailoRT + PyHailoRT):
#    sudo apt update && sudo apt full-upgrade -y
#    sudo apt install -y hailo-all
#    sudo reboot
#
# 2. Clone and setup Hailo Application Code Examples (for HEF models):
#    cd ~
#    git clone https://github.com/hailo-ai/Hailo-Application-Code-Examples.git
#    cd Hailo-Application-Code-Examples/runtime/hailo-8/python/speech_recognition
#    python3 setup.py
#
# 3. The setup.py script above downloads:
#    - HEF model files (Whisper tiny & base for Hailo-8L)
#    - Tokenization assets
#    - Required dependencies (transformers, torch, sounddevice)
#
# 4. transcribe-halo.py automatically imports official Hailo modules:
#    - HailoWhisperPipeline (inference)
#    - Audio preprocessing and tokenization
#    - No additional pip install needed
#
# Notes:
#  - transcribe.py: CPU-based faster-whisper (no Hailo required)
#  - transcribe-halo.py: Hailo AI HAT (requires hailo-all + setup.py)
#
# For detailed setup instructions, see: HAILO_SETUP.md
# ============================================================================
